# HAP MCP credentials
#
# 这是一个示例模板。请复制到 ~/.config/hap-mcp/credentials 后填入真实值。
#
# 如何获取 account / password 加密字符串：
#   1. Chrome 打开 https://www.mingdao.com/
#   2. F12 打开 DevTools → Network 面板 → 勾选 Preserve log
#   3. 用你自己的账号正常登录
#   4. 在 Network 里找 `MDAccountLogin` 请求 → 右侧 Payload
#   5. 把 `account` 和 `password` 两个字段的完整加密字符串复制到下方
#
# 注意：
#   - 这些字符串是前端加密后的 payload，不是明文，直接复制即可
#   - 加密参数会随明道前端版本变化，如果脚本鉴权失败请重新抓取

HAP_LOGIN_ACCOUNT=''
HAP_LOGIN_PASSWORD=''
