{
  "mcpServers": {
    "oldoa": {
      "type": "stdio",
      "command": "python3",
      "args": ["-m", "oldoa.server"],
      "env": {
        "PYTHONPATH": "${CLAUDE_PLUGIN_ROOT}/server/src",
        "OLDOA_CONFIG_DIR": "${OLDOA_CONFIG_DIR:-${HOME}/.config/oldoa}"
      }
    }
  }
}
